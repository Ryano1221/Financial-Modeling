/**
 * Bump this on each frontend deploy so users can confirm they have the latest build
 * (hard refresh / clear site data if footer or console version is stale).
 */
export const FRONTEND_BUILD_VERSION = "20250216.3";
