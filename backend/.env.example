# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lease_deck

# Clerk (JWT issuer from Clerk Dashboard → JWT Templates; JWKS from same domain)
CLERK_JWT_ISSUER=https://your-clerk-domain.clerk.accounts.dev
# Optional: override JWKS URL
# CLERK_JWKS_URL=https://your-clerk-domain.clerk.accounts.dev/.well-known/jwks.json
# For Clerk webhooks (user/org sync)
CLERK_WEBHOOK_SECRET=whsec_...

# Stripe (usage-based billing: runs + PDF exports)
STRIPE_SECRET_KEY=sk_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_METERED_PRICE_ID=price_...
# Optional meter event names (defaults: runs, pdf_exports)
# STRIPE_METER_RUNS=runs
# STRIPE_METER_PDF_EXPORTS=pdf_exports

# S3 (PDFs and uploaded leases)
S3_BUCKET=your-bucket
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY from env or IAM role

# Background jobs (Celery)
REDIS_URL=redis://localhost:6379/0

# Report PDF generation (URL the worker opens to render)
REPORT_BASE_URL=http://localhost:3000

# Optional: OpenAI for lease extraction
# OPENAI_API_KEY=sk-...
