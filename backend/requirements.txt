# Backend dependencies for Render (build: pip install -r requirements.txt from /backend)
# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Lease extraction (PDF/DOCX + LLM)
pypdf>=4.0.0
openai>=1.0.0
python-docx>=1.0.0
# OCR (optional; needs tesseract/poppler on system for scanned PDFs)
pdf2image>=1.17.0
pytesseract>=0.3.10

# PDF report generation (optional; POST /report needs Chromium)
playwright>=1.49.0

# DB (optional; for /api/v1 routes)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Auth (optional; for Clerk JWT)
PyJWT>=2.8.0

# Billing & storage (optional)
stripe>=8.0.0
boto3>=1.34.0
